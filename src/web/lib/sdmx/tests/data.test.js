import sdmxData from '../../../../mock/data/sdmxData';
import { dataParser } from '../';

const series = [];

describe('lib', () => {
  describe('sdmx', () => {
    describe('data', () => {
      /*it.only('should parse sdmx data into series', () => {
        expect(
          dataParser({ locale: 'en' })(sdmxData),
        ).toEqual(series);
      });*/
    });
  });
});
